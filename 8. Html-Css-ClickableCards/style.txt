#htmlContent {
    display: flex;
    flex-wrap: wrap;
    gap: 38px;
}

.bullet-chart-container {
    display: flex;
    flex-direction: column;
    gap: 8px;
    font-family: Segoe UI, sans-serif;
}

.bullet-chart {
    display: grid;
    grid-template-columns: 90px 70px auto; /* Fixed widths for label, value, and flexible chart */
    align-items: center;
    gap: 0px;
    margin-left: 15px;
}